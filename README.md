# Submitting a Form with Fetch API
The request is sent to a PHP file on my server.

## To clone the repo run the following command:

```bash
   git clone https://github.com/dimianni/form-submittion.git
```
